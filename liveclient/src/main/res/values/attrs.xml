<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="SwitchButton">
        <!-- Drawable to use as the "thumb" that switches back and forth. -->
        <attr name="appThumb" format="reference" />
        <!-- Drawable to use as the "track" that the switch thumb slides within. -->
        <attr name="appTrack" format="reference" />
        <!-- Text to use when the switch is in the checked/"on" state. -->
        <attr name="appTextOn" format="string" />
        <!-- Text to use when the switch is in the unchecked/"off" state. -->
        <attr name="appTextOff" format="string" />
        <!-- Amount of padding on either side of text within the switch thumb. -->
        <attr name="appThumbTextPadding" format="dimension" />
        <!-- TextAppearance style for text displayed on the switch thumb. -->
        <attr name="appSwitchTextAppearance" format="reference" />
        <!-- Minimum width for the switch component -->
        <attr name="appSwitchMinWidth" format="dimension" />
        <!-- Minimum space between the switch and caption text -->
        <attr name="appSwitchPadding" format="dimension" />
    </declare-styleable>
    <declare-styleable name="TextAppearance">
        <!-- Text color. -->
        <attr name="switchTextColor" format="reference|color" />
        <!-- Size of the text. Recommended dimension type for text is "sp" for scaled-pixels (example: 15sp). -->
        <attr name="switchTextSize" format="dimension" />
        <!-- Style (bold, italic, bolditalic) for the text. -->
        <attr name="switchTextStyle">
            <flag name="normal" value="0" />
            <flag name="bold" value="1" />
            <flag name="italic" value="2" />
        </attr>
        <!-- Typeface (normal, sans, serif, monospace) for the text. -->
        <attr name="switchTypeface">
            <enum name="normal" value="0" />
            <enum name="sans" value="1" />
            <enum name="serif" value="2" />
            <enum name="monospace" value="3" />
        </attr>
        <!-- Color of the text selection highlight. -->
        <attr name="textColorHighlight" format="reference|color" />
        <!-- Color of the hint text. -->
        <attr name="textColorHint" format="reference|color" />
        <!-- Color of the links. -->
        <attr name="textColorLink" format="reference|color" />
    </declare-styleable>

    <declare-styleable name="ClipImageSize">
        <attr name="clip_image_height" format="integer" />
    </declare-styleable>
</resources>