// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: mic_room.proto
package com.zq.live.proto.Room;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import com.zq.live.proto.Common.UserInfo;
import java.io.IOException;
import java.lang.Long;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class MJoinNoticeMsg extends Message<MJoinNoticeMsg, MJoinNoticeMsg.Builder> {
  public static final ProtoAdapter<MJoinNoticeMsg> ADAPTER = new ProtoAdapter_MJoinNoticeMsg();

  private static final long serialVersionUID = 0L;

  public static final EMUserRole DEFAULT_ROLE = EMUserRole.MRUR_UNKNOWN;

  public static final Long DEFAULT_JOINTIMEMS = 0L;

  /**
   * 加入游戏的信息
   */
  @WireField(
      tag = 1,
      adapter = "com.zq.live.proto.Common.UserInfo#ADAPTER"
  )
  private final UserInfo userInfo;

  /**
   * 用户角色
   */
  @WireField(
      tag = 2,
      adapter = "com.zq.live.proto.Room.EMUserRole#ADAPTER"
  )
  private final EMUserRole role;

  /**
   * 加入毫秒时间戳
   */
  @WireField(
      tag = 3,
      adapter = "com.squareup.wire.ProtoAdapter#SINT64"
  )
  private final Long joinTimeMs;

  public MJoinNoticeMsg(UserInfo userInfo, EMUserRole role, Long joinTimeMs) {
    this(userInfo, role, joinTimeMs, ByteString.EMPTY);
  }

  public MJoinNoticeMsg(UserInfo userInfo, EMUserRole role, Long joinTimeMs,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userInfo = userInfo;
    this.role = role;
    this.joinTimeMs = joinTimeMs;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userInfo = userInfo;
    builder.role = role;
    builder.joinTimeMs = joinTimeMs;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof MJoinNoticeMsg)) return false;
    MJoinNoticeMsg o = (MJoinNoticeMsg) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(userInfo, o.userInfo)
        && Internal.equals(role, o.role)
        && Internal.equals(joinTimeMs, o.joinTimeMs);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (userInfo != null ? userInfo.hashCode() : 0);
      result = result * 37 + (role != null ? role.hashCode() : 0);
      result = result * 37 + (joinTimeMs != null ? joinTimeMs.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (userInfo != null) builder.append(", userInfo=").append(userInfo);
    if (role != null) builder.append(", role=").append(role);
    if (joinTimeMs != null) builder.append(", joinTimeMs=").append(joinTimeMs);
    return builder.replace(0, 2, "MJoinNoticeMsg{").append('}').toString();
  }

  public byte[] toByteArray() {
    return MJoinNoticeMsg.ADAPTER.encode(this);
  }

  public static final MJoinNoticeMsg parseFrom(byte[] data) throws IOException {
    MJoinNoticeMsg c = null;
       c = MJoinNoticeMsg.ADAPTER.decode(data);
    return c;
  }

  /**
   * 加入游戏的信息
   */
  public UserInfo getUserInfo() {
    if(userInfo==null){
        return new UserInfo.Builder().build();
    }
    return userInfo;
  }

  /**
   * 用户角色
   */
  public EMUserRole getRole() {
    if(role==null){
        return new EMUserRole.Builder().build();
    }
    return role;
  }

  /**
   * 加入毫秒时间戳
   */
  public Long getJoinTimeMs() {
    if(joinTimeMs==null){
        return DEFAULT_JOINTIMEMS;
    }
    return joinTimeMs;
  }

  /**
   * 加入游戏的信息
   */
  public boolean hasUserInfo() {
    return userInfo!=null;
  }

  /**
   * 用户角色
   */
  public boolean hasRole() {
    return role!=null;
  }

  /**
   * 加入毫秒时间戳
   */
  public boolean hasJoinTimeMs() {
    return joinTimeMs!=null;
  }

  public static final class Builder extends Message.Builder<MJoinNoticeMsg, Builder> {
    private UserInfo userInfo;

    private EMUserRole role;

    private Long joinTimeMs;

    public Builder() {
    }

    /**
     * 加入游戏的信息
     */
    public Builder setUserInfo(UserInfo userInfo) {
      this.userInfo = userInfo;
      return this;
    }

    /**
     * 用户角色
     */
    public Builder setRole(EMUserRole role) {
      this.role = role;
      return this;
    }

    /**
     * 加入毫秒时间戳
     */
    public Builder setJoinTimeMs(Long joinTimeMs) {
      this.joinTimeMs = joinTimeMs;
      return this;
    }

    @Override
    public MJoinNoticeMsg build() {
      return new MJoinNoticeMsg(userInfo, role, joinTimeMs, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_MJoinNoticeMsg extends ProtoAdapter<MJoinNoticeMsg> {
    public ProtoAdapter_MJoinNoticeMsg() {
      super(FieldEncoding.LENGTH_DELIMITED, MJoinNoticeMsg.class);
    }

    @Override
    public int encodedSize(MJoinNoticeMsg value) {
      return UserInfo.ADAPTER.encodedSizeWithTag(1, value.userInfo)
          + EMUserRole.ADAPTER.encodedSizeWithTag(2, value.role)
          + ProtoAdapter.SINT64.encodedSizeWithTag(3, value.joinTimeMs)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, MJoinNoticeMsg value) throws IOException {
      UserInfo.ADAPTER.encodeWithTag(writer, 1, value.userInfo);
      EMUserRole.ADAPTER.encodeWithTag(writer, 2, value.role);
      ProtoAdapter.SINT64.encodeWithTag(writer, 3, value.joinTimeMs);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public MJoinNoticeMsg decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setUserInfo(UserInfo.ADAPTER.decode(reader)); break;
          case 2: {
            try {
              builder.setRole(EMUserRole.ADAPTER.decode(reader));
            } catch (ProtoAdapter.EnumConstantNotFoundException e) {
              builder.addUnknownField(tag, FieldEncoding.VARINT, (long) e.value);
            }
            break;
          }
          case 3: builder.setJoinTimeMs(ProtoAdapter.SINT64.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public MJoinNoticeMsg redact(MJoinNoticeMsg value) {
      Builder builder = value.newBuilder();
      if (builder.userInfo != null) builder.userInfo = UserInfo.ADAPTER.redact(builder.userInfo);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
