// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: party_room.proto
package com.zq.live.proto.PartyRoom;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import com.zq.live.proto.Common.POnlineInfo;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class PGetSeatMsg extends Message<PGetSeatMsg, PGetSeatMsg.Builder> {
  public static final ProtoAdapter<PGetSeatMsg> ADAPTER = new ProtoAdapter_PGetSeatMsg();

  private static final long serialVersionUID = 0L;

  /**
   * 用户信息
   */
  @WireField(
      tag = 1,
      adapter = "com.zq.live.proto.Common.POnlineInfo#ADAPTER"
  )
  private final POnlineInfo user;

  /**
   * 座位信息
   */
  @WireField(
      tag = 2,
      adapter = "com.zq.live.proto.PartyRoom.SeatInfo#ADAPTER"
  )
  private final SeatInfo seatInfo;

  public PGetSeatMsg(POnlineInfo user, SeatInfo seatInfo) {
    this(user, seatInfo, ByteString.EMPTY);
  }

  public PGetSeatMsg(POnlineInfo user, SeatInfo seatInfo, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.user = user;
    this.seatInfo = seatInfo;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.user = user;
    builder.seatInfo = seatInfo;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof PGetSeatMsg)) return false;
    PGetSeatMsg o = (PGetSeatMsg) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(user, o.user)
        && Internal.equals(seatInfo, o.seatInfo);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (user != null ? user.hashCode() : 0);
      result = result * 37 + (seatInfo != null ? seatInfo.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (user != null) builder.append(", user=").append(user);
    if (seatInfo != null) builder.append(", seatInfo=").append(seatInfo);
    return builder.replace(0, 2, "PGetSeatMsg{").append('}').toString();
  }

  public byte[] toByteArray() {
    return PGetSeatMsg.ADAPTER.encode(this);
  }

  public static final PGetSeatMsg parseFrom(byte[] data) throws IOException {
    PGetSeatMsg c = null;
       c = PGetSeatMsg.ADAPTER.decode(data);
    return c;
  }

  /**
   * 用户信息
   */
  public POnlineInfo getUser() {
    if(user==null){
        return new POnlineInfo.Builder().build();
    }
    return user;
  }

  /**
   * 座位信息
   */
  public SeatInfo getSeatInfo() {
    if(seatInfo==null){
        return new SeatInfo.Builder().build();
    }
    return seatInfo;
  }

  /**
   * 用户信息
   */
  public boolean hasUser() {
    return user!=null;
  }

  /**
   * 座位信息
   */
  public boolean hasSeatInfo() {
    return seatInfo!=null;
  }

  public static final class Builder extends Message.Builder<PGetSeatMsg, Builder> {
    private POnlineInfo user;

    private SeatInfo seatInfo;

    public Builder() {
    }

    /**
     * 用户信息
     */
    public Builder setUser(POnlineInfo user) {
      this.user = user;
      return this;
    }

    /**
     * 座位信息
     */
    public Builder setSeatInfo(SeatInfo seatInfo) {
      this.seatInfo = seatInfo;
      return this;
    }

    @Override
    public PGetSeatMsg build() {
      return new PGetSeatMsg(user, seatInfo, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_PGetSeatMsg extends ProtoAdapter<PGetSeatMsg> {
    public ProtoAdapter_PGetSeatMsg() {
      super(FieldEncoding.LENGTH_DELIMITED, PGetSeatMsg.class);
    }

    @Override
    public int encodedSize(PGetSeatMsg value) {
      return POnlineInfo.ADAPTER.encodedSizeWithTag(1, value.user)
          + SeatInfo.ADAPTER.encodedSizeWithTag(2, value.seatInfo)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, PGetSeatMsg value) throws IOException {
      POnlineInfo.ADAPTER.encodeWithTag(writer, 1, value.user);
      SeatInfo.ADAPTER.encodeWithTag(writer, 2, value.seatInfo);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public PGetSeatMsg decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setUser(POnlineInfo.ADAPTER.decode(reader)); break;
          case 2: builder.setSeatInfo(SeatInfo.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public PGetSeatMsg redact(PGetSeatMsg value) {
      Builder builder = value.newBuilder();
      if (builder.user != null) builder.user = POnlineInfo.ADAPTER.redact(builder.user);
      if (builder.seatInfo != null) builder.seatInfo = SeatInfo.ADAPTER.redact(builder.seatInfo);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
