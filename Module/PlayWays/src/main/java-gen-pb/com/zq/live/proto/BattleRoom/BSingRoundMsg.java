// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: battle_room.proto
package com.zq.live.proto.BattleRoom;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class BSingRoundMsg extends Message<BSingRoundMsg, BSingRoundMsg.Builder> {
  public static final ProtoAdapter<BSingRoundMsg> ADAPTER = new ProtoAdapter_BSingRoundMsg();

  private static final long serialVersionUID = 0L;

  /**
   * 当前结束轮次的信息
   */
  @WireField(
      tag = 1,
      adapter = "com.zq.live.proto.BattleRoom.BRoundInfo#ADAPTER"
  )
  private final BRoundInfo currentRound;

  public BSingRoundMsg(BRoundInfo currentRound) {
    this(currentRound, ByteString.EMPTY);
  }

  public BSingRoundMsg(BRoundInfo currentRound, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.currentRound = currentRound;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.currentRound = currentRound;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof BSingRoundMsg)) return false;
    BSingRoundMsg o = (BSingRoundMsg) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(currentRound, o.currentRound);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (currentRound != null ? currentRound.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (currentRound != null) builder.append(", currentRound=").append(currentRound);
    return builder.replace(0, 2, "BSingRoundMsg{").append('}').toString();
  }

  public byte[] toByteArray() {
    return BSingRoundMsg.ADAPTER.encode(this);
  }

  public static final BSingRoundMsg parseFrom(byte[] data) throws IOException {
    BSingRoundMsg c = null;
       c = BSingRoundMsg.ADAPTER.decode(data);
    return c;
  }

  /**
   * 当前结束轮次的信息
   */
  public BRoundInfo getCurrentRound() {
    if(currentRound==null){
        return new BRoundInfo.Builder().build();
    }
    return currentRound;
  }

  /**
   * 当前结束轮次的信息
   */
  public boolean hasCurrentRound() {
    return currentRound!=null;
  }

  public static final class Builder extends Message.Builder<BSingRoundMsg, Builder> {
    private BRoundInfo currentRound;

    public Builder() {
    }

    /**
     * 当前结束轮次的信息
     */
    public Builder setCurrentRound(BRoundInfo currentRound) {
      this.currentRound = currentRound;
      return this;
    }

    @Override
    public BSingRoundMsg build() {
      return new BSingRoundMsg(currentRound, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_BSingRoundMsg extends ProtoAdapter<BSingRoundMsg> {
    public ProtoAdapter_BSingRoundMsg() {
      super(FieldEncoding.LENGTH_DELIMITED, BSingRoundMsg.class);
    }

    @Override
    public int encodedSize(BSingRoundMsg value) {
      return BRoundInfo.ADAPTER.encodedSizeWithTag(1, value.currentRound)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, BSingRoundMsg value) throws IOException {
      BRoundInfo.ADAPTER.encodeWithTag(writer, 1, value.currentRound);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public BSingRoundMsg decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setCurrentRound(BRoundInfo.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public BSingRoundMsg redact(BSingRoundMsg value) {
      Builder builder = value.newBuilder();
      if (builder.currentRound != null) builder.currentRound = BRoundInfo.ADAPTER.redact(builder.currentRound);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
