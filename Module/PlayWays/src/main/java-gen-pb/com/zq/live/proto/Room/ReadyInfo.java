// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: Room.proto
package com.zq.live.proto.Room;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Integer;
import java.lang.Long;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class ReadyInfo extends Message<ReadyInfo, ReadyInfo.Builder> {
  public static final ProtoAdapter<ReadyInfo> ADAPTER = new ProtoAdapter_ReadyInfo();

  private static final long serialVersionUID = 0L;

  public static final Integer DEFAULT_USERID = 0;

  public static final Integer DEFAULT_READYSEQ = 0;

  public static final Long DEFAULT_READYTIMEMS = 0L;

  /**
   * 用户id
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#UINT32"
  )
  private final Integer userID;

  /**
   * 准备顺序
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#UINT32"
  )
  private final Integer readySeq;

  /**
   * 准备时间戳
   */
  @WireField(
      tag = 3,
      adapter = "com.squareup.wire.ProtoAdapter#SINT64"
  )
  private final Long readyTimeMs;

  public ReadyInfo(Integer userID, Integer readySeq, Long readyTimeMs) {
    this(userID, readySeq, readyTimeMs, ByteString.EMPTY);
  }

  public ReadyInfo(Integer userID, Integer readySeq, Long readyTimeMs, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userID = userID;
    this.readySeq = readySeq;
    this.readyTimeMs = readyTimeMs;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userID = userID;
    builder.readySeq = readySeq;
    builder.readyTimeMs = readyTimeMs;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof ReadyInfo)) return false;
    ReadyInfo o = (ReadyInfo) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(userID, o.userID)
        && Internal.equals(readySeq, o.readySeq)
        && Internal.equals(readyTimeMs, o.readyTimeMs);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (userID != null ? userID.hashCode() : 0);
      result = result * 37 + (readySeq != null ? readySeq.hashCode() : 0);
      result = result * 37 + (readyTimeMs != null ? readyTimeMs.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (userID != null) builder.append(", userID=").append(userID);
    if (readySeq != null) builder.append(", readySeq=").append(readySeq);
    if (readyTimeMs != null) builder.append(", readyTimeMs=").append(readyTimeMs);
    return builder.replace(0, 2, "ReadyInfo{").append('}').toString();
  }

  public byte[] toByteArray() {
    return ReadyInfo.ADAPTER.encode(this);
  }

  public static final ReadyInfo parseFrom(byte[] data) throws IOException {
    ReadyInfo c = null;
       c = ReadyInfo.ADAPTER.decode(data);
    return c;
  }

  /**
   * 用户id
   */
  public Integer getUserID() {
    if(userID==null){
        return DEFAULT_USERID;
    }
    return userID;
  }

  /**
   * 准备顺序
   */
  public Integer getReadySeq() {
    if(readySeq==null){
        return DEFAULT_READYSEQ;
    }
    return readySeq;
  }

  /**
   * 准备时间戳
   */
  public Long getReadyTimeMs() {
    if(readyTimeMs==null){
        return DEFAULT_READYTIMEMS;
    }
    return readyTimeMs;
  }

  /**
   * 用户id
   */
  public boolean hasUserID() {
    return userID!=null;
  }

  /**
   * 准备顺序
   */
  public boolean hasReadySeq() {
    return readySeq!=null;
  }

  /**
   * 准备时间戳
   */
  public boolean hasReadyTimeMs() {
    return readyTimeMs!=null;
  }

  public static final class Builder extends Message.Builder<ReadyInfo, Builder> {
    private Integer userID;

    private Integer readySeq;

    private Long readyTimeMs;

    public Builder() {
    }

    /**
     * 用户id
     */
    public Builder setUserID(Integer userID) {
      this.userID = userID;
      return this;
    }

    /**
     * 准备顺序
     */
    public Builder setReadySeq(Integer readySeq) {
      this.readySeq = readySeq;
      return this;
    }

    /**
     * 准备时间戳
     */
    public Builder setReadyTimeMs(Long readyTimeMs) {
      this.readyTimeMs = readyTimeMs;
      return this;
    }

    @Override
    public ReadyInfo build() {
      return new ReadyInfo(userID, readySeq, readyTimeMs, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_ReadyInfo extends ProtoAdapter<ReadyInfo> {
    public ProtoAdapter_ReadyInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, ReadyInfo.class);
    }

    @Override
    public int encodedSize(ReadyInfo value) {
      return ProtoAdapter.UINT32.encodedSizeWithTag(1, value.userID)
          + ProtoAdapter.UINT32.encodedSizeWithTag(2, value.readySeq)
          + ProtoAdapter.SINT64.encodedSizeWithTag(3, value.readyTimeMs)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, ReadyInfo value) throws IOException {
      ProtoAdapter.UINT32.encodeWithTag(writer, 1, value.userID);
      ProtoAdapter.UINT32.encodeWithTag(writer, 2, value.readySeq);
      ProtoAdapter.SINT64.encodeWithTag(writer, 3, value.readyTimeMs);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public ReadyInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setUserID(ProtoAdapter.UINT32.decode(reader)); break;
          case 2: builder.setReadySeq(ProtoAdapter.UINT32.decode(reader)); break;
          case 3: builder.setReadyTimeMs(ProtoAdapter.SINT64.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public ReadyInfo redact(ReadyInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
