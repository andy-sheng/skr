// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: battle_room.proto
package com.zq.live.proto.BattleRoom;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Long;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class BNextRoundMsg extends Message<BNextRoundMsg, BNextRoundMsg.Builder> {
  public static final ProtoAdapter<BNextRoundMsg> ADAPTER = new ProtoAdapter_BNextRoundMsg();

  private static final long serialVersionUID = 0L;

  public static final Long DEFAULT_ROUNDOVERTIMEMS = 0L;

  /**
   * 本轮次结束的毫秒时间戳
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#SINT64"
  )
  private final Long roundOverTimeMs;

  /**
   * 当前结束轮次的信息
   */
  @WireField(
      tag = 2,
      adapter = "com.zq.live.proto.BattleRoom.BRoundInfo#ADAPTER"
  )
  private final BRoundInfo currentRound;

  /**
   * 下个加载轮次的信息
   */
  @WireField(
      tag = 3,
      adapter = "com.zq.live.proto.BattleRoom.BRoundInfo#ADAPTER"
  )
  private final BRoundInfo nextRound;

  public BNextRoundMsg(Long roundOverTimeMs, BRoundInfo currentRound, BRoundInfo nextRound) {
    this(roundOverTimeMs, currentRound, nextRound, ByteString.EMPTY);
  }

  public BNextRoundMsg(Long roundOverTimeMs, BRoundInfo currentRound, BRoundInfo nextRound,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.roundOverTimeMs = roundOverTimeMs;
    this.currentRound = currentRound;
    this.nextRound = nextRound;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.roundOverTimeMs = roundOverTimeMs;
    builder.currentRound = currentRound;
    builder.nextRound = nextRound;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof BNextRoundMsg)) return false;
    BNextRoundMsg o = (BNextRoundMsg) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(roundOverTimeMs, o.roundOverTimeMs)
        && Internal.equals(currentRound, o.currentRound)
        && Internal.equals(nextRound, o.nextRound);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (roundOverTimeMs != null ? roundOverTimeMs.hashCode() : 0);
      result = result * 37 + (currentRound != null ? currentRound.hashCode() : 0);
      result = result * 37 + (nextRound != null ? nextRound.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (roundOverTimeMs != null) builder.append(", roundOverTimeMs=").append(roundOverTimeMs);
    if (currentRound != null) builder.append(", currentRound=").append(currentRound);
    if (nextRound != null) builder.append(", nextRound=").append(nextRound);
    return builder.replace(0, 2, "BNextRoundMsg{").append('}').toString();
  }

  public byte[] toByteArray() {
    return BNextRoundMsg.ADAPTER.encode(this);
  }

  public static final BNextRoundMsg parseFrom(byte[] data) throws IOException {
    BNextRoundMsg c = null;
       c = BNextRoundMsg.ADAPTER.decode(data);
    return c;
  }

  /**
   * 本轮次结束的毫秒时间戳
   */
  public Long getRoundOverTimeMs() {
    if(roundOverTimeMs==null){
        return DEFAULT_ROUNDOVERTIMEMS;
    }
    return roundOverTimeMs;
  }

  /**
   * 当前结束轮次的信息
   */
  public BRoundInfo getCurrentRound() {
    if(currentRound==null){
        return new BRoundInfo.Builder().build();
    }
    return currentRound;
  }

  /**
   * 下个加载轮次的信息
   */
  public BRoundInfo getNextRound() {
    if(nextRound==null){
        return new BRoundInfo.Builder().build();
    }
    return nextRound;
  }

  /**
   * 本轮次结束的毫秒时间戳
   */
  public boolean hasRoundOverTimeMs() {
    return roundOverTimeMs!=null;
  }

  /**
   * 当前结束轮次的信息
   */
  public boolean hasCurrentRound() {
    return currentRound!=null;
  }

  /**
   * 下个加载轮次的信息
   */
  public boolean hasNextRound() {
    return nextRound!=null;
  }

  public static final class Builder extends Message.Builder<BNextRoundMsg, Builder> {
    private Long roundOverTimeMs;

    private BRoundInfo currentRound;

    private BRoundInfo nextRound;

    public Builder() {
    }

    /**
     * 本轮次结束的毫秒时间戳
     */
    public Builder setRoundOverTimeMs(Long roundOverTimeMs) {
      this.roundOverTimeMs = roundOverTimeMs;
      return this;
    }

    /**
     * 当前结束轮次的信息
     */
    public Builder setCurrentRound(BRoundInfo currentRound) {
      this.currentRound = currentRound;
      return this;
    }

    /**
     * 下个加载轮次的信息
     */
    public Builder setNextRound(BRoundInfo nextRound) {
      this.nextRound = nextRound;
      return this;
    }

    @Override
    public BNextRoundMsg build() {
      return new BNextRoundMsg(roundOverTimeMs, currentRound, nextRound, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_BNextRoundMsg extends ProtoAdapter<BNextRoundMsg> {
    public ProtoAdapter_BNextRoundMsg() {
      super(FieldEncoding.LENGTH_DELIMITED, BNextRoundMsg.class);
    }

    @Override
    public int encodedSize(BNextRoundMsg value) {
      return ProtoAdapter.SINT64.encodedSizeWithTag(1, value.roundOverTimeMs)
          + BRoundInfo.ADAPTER.encodedSizeWithTag(2, value.currentRound)
          + BRoundInfo.ADAPTER.encodedSizeWithTag(3, value.nextRound)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, BNextRoundMsg value) throws IOException {
      ProtoAdapter.SINT64.encodeWithTag(writer, 1, value.roundOverTimeMs);
      BRoundInfo.ADAPTER.encodeWithTag(writer, 2, value.currentRound);
      BRoundInfo.ADAPTER.encodeWithTag(writer, 3, value.nextRound);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public BNextRoundMsg decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setRoundOverTimeMs(ProtoAdapter.SINT64.decode(reader)); break;
          case 2: builder.setCurrentRound(BRoundInfo.ADAPTER.decode(reader)); break;
          case 3: builder.setNextRound(BRoundInfo.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public BNextRoundMsg redact(BNextRoundMsg value) {
      Builder builder = value.newBuilder();
      if (builder.currentRound != null) builder.currentRound = BRoundInfo.ADAPTER.redact(builder.currentRound);
      if (builder.nextRound != null) builder.nextRound = BRoundInfo.ADAPTER.redact(builder.nextRound);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
