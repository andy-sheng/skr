// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: CombineRoom.proto
package com.zq.live.proto.CombineRoom;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import com.zq.live.proto.Common.GamePanelInfo;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class EndGameMsg extends Message<EndGameMsg, EndGameMsg.Builder> {
  public static final ProtoAdapter<EndGameMsg> ADAPTER = new ProtoAdapter_EndGameMsg();

  private static final long serialVersionUID = 0L;

  /**
   * 当前游戏结束后的下个面板
   */
  @WireField(
      tag = 1,
      adapter = "com.zq.live.proto.Common.GamePanelInfo#ADAPTER"
  )
  private final GamePanelInfo nextPanel;

  public EndGameMsg(GamePanelInfo nextPanel) {
    this(nextPanel, ByteString.EMPTY);
  }

  public EndGameMsg(GamePanelInfo nextPanel, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.nextPanel = nextPanel;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.nextPanel = nextPanel;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof EndGameMsg)) return false;
    EndGameMsg o = (EndGameMsg) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(nextPanel, o.nextPanel);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (nextPanel != null ? nextPanel.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (nextPanel != null) builder.append(", nextPanel=").append(nextPanel);
    return builder.replace(0, 2, "EndGameMsg{").append('}').toString();
  }

  public byte[] toByteArray() {
    return EndGameMsg.ADAPTER.encode(this);
  }

  public static final EndGameMsg parseFrom(byte[] data) throws IOException {
    EndGameMsg c = null;
       c = EndGameMsg.ADAPTER.decode(data);
    return c;
  }

  /**
   * 当前游戏结束后的下个面板
   */
  public GamePanelInfo getNextPanel() {
    if(nextPanel==null){
        return new GamePanelInfo.Builder().build();
    }
    return nextPanel;
  }

  /**
   * 当前游戏结束后的下个面板
   */
  public boolean hasNextPanel() {
    return nextPanel!=null;
  }

  public static final class Builder extends Message.Builder<EndGameMsg, Builder> {
    private GamePanelInfo nextPanel;

    public Builder() {
    }

    /**
     * 当前游戏结束后的下个面板
     */
    public Builder setNextPanel(GamePanelInfo nextPanel) {
      this.nextPanel = nextPanel;
      return this;
    }

    @Override
    public EndGameMsg build() {
      return new EndGameMsg(nextPanel, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_EndGameMsg extends ProtoAdapter<EndGameMsg> {
    public ProtoAdapter_EndGameMsg() {
      super(FieldEncoding.LENGTH_DELIMITED, EndGameMsg.class);
    }

    @Override
    public int encodedSize(EndGameMsg value) {
      return GamePanelInfo.ADAPTER.encodedSizeWithTag(1, value.nextPanel)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, EndGameMsg value) throws IOException {
      GamePanelInfo.ADAPTER.encodeWithTag(writer, 1, value.nextPanel);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public EndGameMsg decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setNextPanel(GamePanelInfo.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public EndGameMsg redact(EndGameMsg value) {
      Builder builder = value.newBuilder();
      if (builder.nextPanel != null) builder.nextPanel = GamePanelInfo.ADAPTER.redact(builder.nextPanel);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
