// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: HotChannel.proto
package com.wali.live.proto.HotChannel;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import com.wali.live.proto.CommonChannel.ChannelItem;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class TabPage extends Message<TabPage, TabPage.Builder> {
  public static final ProtoAdapter<TabPage> ADAPTER = new ProtoAdapter_TabPage();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_NAME = "";

  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String name;

  @WireField(
      tag = 2,
      adapter = "com.wali.live.proto.CommonChannel.ChannelItem#ADAPTER",
      label = WireField.Label.REPEATED
  )
  public final List<ChannelItem> items;

  public TabPage(String name, List<ChannelItem> items) {
    this(name, items, ByteString.EMPTY);
  }

  public TabPage(String name, List<ChannelItem> items, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.name = name;
    this.items = Internal.immutableCopyOf("items", items);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.name = name;
    builder.items = Internal.copyOf("items", items);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof TabPage)) return false;
    TabPage o = (TabPage) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(name, o.name)
        && items.equals(o.items);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (name != null ? name.hashCode() : 0);
      result = result * 37 + items.hashCode();
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (name != null) builder.append(", name=").append(name);
    if (!items.isEmpty()) builder.append(", items=").append(items);
    return builder.replace(0, 2, "TabPage{").append('}').toString();
  }

  public byte[] toByteArray() {
    return TabPage.ADAPTER.encode(this);
  }

  public static final TabPage parseFrom(byte[] data) throws IOException {
    TabPage c = null;
       c = TabPage.ADAPTER.decode(data);
    return c;
  }

  public String getName() {
    if(name==null){
        return DEFAULT_NAME;
    }
    return name;
  }

  public List<ChannelItem> getItemsList() {
    if(items==null){
        return new java.util.ArrayList<ChannelItem>();
    }
    return items;
  }

  public boolean hasName() {
    return name!=null;
  }

  public boolean hasItemsList() {
    return items!=null;
  }

  public static final class Builder extends Message.Builder<TabPage, Builder> {
    public String name;

    public List<ChannelItem> items;

    public Builder() {
      items = Internal.newMutableList();
    }

    public Builder setName(String name) {
      this.name = name;
      return this;
    }

    public Builder addAllItems(List<ChannelItem> items) {
      Internal.checkElementsNotNull(items);
      this.items = items;
      return this;
    }

    @Override
    public TabPage build() {
      return new TabPage(name, items, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_TabPage extends ProtoAdapter<TabPage> {
    public ProtoAdapter_TabPage() {
      super(FieldEncoding.LENGTH_DELIMITED, TabPage.class);
    }

    @Override
    public int encodedSize(TabPage value) {
      return ProtoAdapter.STRING.encodedSizeWithTag(1, value.name)
          + ChannelItem.ADAPTER.asRepeated().encodedSizeWithTag(2, value.items)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, TabPage value) throws IOException {
      ProtoAdapter.STRING.encodeWithTag(writer, 1, value.name);
      ChannelItem.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.items);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public TabPage decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setName(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.items.add(ChannelItem.ADAPTER.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public TabPage redact(TabPage value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.items, ChannelItem.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
