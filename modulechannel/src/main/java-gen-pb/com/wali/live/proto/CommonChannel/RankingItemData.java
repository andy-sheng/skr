// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: CommonChannel.proto
package com.wali.live.proto.CommonChannel;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class RankingItemData extends Message<RankingItemData, RankingItemData.Builder> {
  public static final ProtoAdapter<RankingItemData> ADAPTER = new ProtoAdapter_RankingItemData();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_JUMPSCHEMEURI = "";

  /**
   * 用户资料
   */
  @WireField(
      tag = 1,
      adapter = "com.wali.live.proto.CommonChannel.UserInfo#ADAPTER"
  )
  public final UserInfo userInfo;

  /**
   * item跳转schemeUri
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String jumpSchemeUri;

  public RankingItemData(UserInfo userInfo, String jumpSchemeUri) {
    this(userInfo, jumpSchemeUri, ByteString.EMPTY);
  }

  public RankingItemData(UserInfo userInfo, String jumpSchemeUri, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userInfo = userInfo;
    this.jumpSchemeUri = jumpSchemeUri;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userInfo = userInfo;
    builder.jumpSchemeUri = jumpSchemeUri;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof RankingItemData)) return false;
    RankingItemData o = (RankingItemData) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(userInfo, o.userInfo)
        && Internal.equals(jumpSchemeUri, o.jumpSchemeUri);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (userInfo != null ? userInfo.hashCode() : 0);
      result = result * 37 + (jumpSchemeUri != null ? jumpSchemeUri.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (userInfo != null) builder.append(", userInfo=").append(userInfo);
    if (jumpSchemeUri != null) builder.append(", jumpSchemeUri=").append(jumpSchemeUri);
    return builder.replace(0, 2, "RankingItemData{").append('}').toString();
  }

  public byte[] toByteArray() {
    return RankingItemData.ADAPTER.encode(this);
  }

  public static final RankingItemData parseFrom(byte[] data) throws IOException {
    RankingItemData c = null;
       c = RankingItemData.ADAPTER.decode(data);
    return c;
  }

  /**
   * 用户资料
   */
  public UserInfo getUserInfo() {
    if(userInfo==null){
        return new UserInfo.Builder().build();
    }
    return userInfo;
  }

  /**
   * item跳转schemeUri
   */
  public String getJumpSchemeUri() {
    if(jumpSchemeUri==null){
        return DEFAULT_JUMPSCHEMEURI;
    }
    return jumpSchemeUri;
  }

  /**
   * 用户资料
   */
  public boolean hasUserInfo() {
    return userInfo!=null;
  }

  /**
   * item跳转schemeUri
   */
  public boolean hasJumpSchemeUri() {
    return jumpSchemeUri!=null;
  }

  public static final class Builder extends Message.Builder<RankingItemData, Builder> {
    public UserInfo userInfo;

    public String jumpSchemeUri;

    public Builder() {
    }

    /**
     * 用户资料
     */
    public Builder setUserInfo(UserInfo userInfo) {
      this.userInfo = userInfo;
      return this;
    }

    /**
     * item跳转schemeUri
     */
    public Builder setJumpSchemeUri(String jumpSchemeUri) {
      this.jumpSchemeUri = jumpSchemeUri;
      return this;
    }

    @Override
    public RankingItemData build() {
      return new RankingItemData(userInfo, jumpSchemeUri, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RankingItemData extends ProtoAdapter<RankingItemData> {
    public ProtoAdapter_RankingItemData() {
      super(FieldEncoding.LENGTH_DELIMITED, RankingItemData.class);
    }

    @Override
    public int encodedSize(RankingItemData value) {
      return UserInfo.ADAPTER.encodedSizeWithTag(1, value.userInfo)
          + ProtoAdapter.STRING.encodedSizeWithTag(2, value.jumpSchemeUri)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, RankingItemData value) throws IOException {
      UserInfo.ADAPTER.encodeWithTag(writer, 1, value.userInfo);
      ProtoAdapter.STRING.encodeWithTag(writer, 2, value.jumpSchemeUri);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RankingItemData decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setUserInfo(UserInfo.ADAPTER.decode(reader)); break;
          case 2: builder.setJumpSchemeUri(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public RankingItemData redact(RankingItemData value) {
      Builder builder = value.newBuilder();
      if (builder.userInfo != null) builder.userInfo = UserInfo.ADAPTER.redact(builder.userInfo);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
