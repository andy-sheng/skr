// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: LiveMessage.proto
package com.wali.live.proto.LiveMessage;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import com.wali.live.proto.LiveCommon.Viewer;
import java.io.IOException;
import java.lang.Boolean;
import java.lang.Integer;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

/**
 * 加入房间
 */
public final class JoinRoomMessage extends Message<JoinRoomMessage, JoinRoomMessage.Builder> {
  public static final ProtoAdapter<JoinRoomMessage> ADAPTER = new ProtoAdapter_JoinRoomMessage();

  private static final long serialVersionUID = 0L;

  public static final Integer DEFAULT_VIEWER_COUNT = 0;

  public static final Integer DEFAULT_TYPE = 0;

  public static final Boolean DEFAULT_SHOWSPECIALEFFECT = false;

  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#UINT32"
  )
  public final Integer viewer_count;

  /**
   * 观众
   */
  @WireField(
      tag = 2,
      adapter = "com.wali.live.proto.LiveCommon.Viewer#ADAPTER",
      label = WireField.Label.REPEATED
  )
  public final List<Viewer> viewers;

  /**
   * 0：默认 1：通过附近频道进入
   */
  @WireField(
      tag = 3,
      adapter = "com.squareup.wire.ProtoAdapter#UINT32"
  )
  public final Integer type;

  /**
   * 进场特效 true:显示
   */
  @WireField(
      tag = 4,
      adapter = "com.squareup.wire.ProtoAdapter#BOOL"
  )
  public final Boolean showSpecialEffect;

  public JoinRoomMessage(Integer viewer_count, List<Viewer> viewers, Integer type,
      Boolean showSpecialEffect) {
    this(viewer_count, viewers, type, showSpecialEffect, ByteString.EMPTY);
  }

  public JoinRoomMessage(Integer viewer_count, List<Viewer> viewers, Integer type,
      Boolean showSpecialEffect, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.viewer_count = viewer_count;
    this.viewers = Internal.immutableCopyOf("viewers", viewers);
    this.type = type;
    this.showSpecialEffect = showSpecialEffect;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.viewer_count = viewer_count;
    builder.viewers = Internal.copyOf("viewers", viewers);
    builder.type = type;
    builder.showSpecialEffect = showSpecialEffect;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof JoinRoomMessage)) return false;
    JoinRoomMessage o = (JoinRoomMessage) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(viewer_count, o.viewer_count)
        && viewers.equals(o.viewers)
        && Internal.equals(type, o.type)
        && Internal.equals(showSpecialEffect, o.showSpecialEffect);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (viewer_count != null ? viewer_count.hashCode() : 0);
      result = result * 37 + viewers.hashCode();
      result = result * 37 + (type != null ? type.hashCode() : 0);
      result = result * 37 + (showSpecialEffect != null ? showSpecialEffect.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (viewer_count != null) builder.append(", viewer_count=").append(viewer_count);
    if (!viewers.isEmpty()) builder.append(", viewers=").append(viewers);
    if (type != null) builder.append(", type=").append(type);
    if (showSpecialEffect != null) builder.append(", showSpecialEffect=").append(showSpecialEffect);
    return builder.replace(0, 2, "JoinRoomMessage{").append('}').toString();
  }

  public byte[] toByteArray() {
    return JoinRoomMessage.ADAPTER.encode(this);
  }

  public static final JoinRoomMessage parseFrom(byte[] data) throws IOException {
    JoinRoomMessage c = null;
       c = JoinRoomMessage.ADAPTER.decode(data);
    return c;
  }

  public Integer getViewerCount() {
    if(viewer_count==null){
        return DEFAULT_VIEWER_COUNT;
    }
    return viewer_count;
  }

  /**
   * 观众
   */
  public List<Viewer> getViewersList() {
    if(viewers==null){
        return new java.util.ArrayList<Viewer>();
    }
    return viewers;
  }

  /**
   * 0：默认 1：通过附近频道进入
   */
  public Integer getType() {
    if(type==null){
        return DEFAULT_TYPE;
    }
    return type;
  }

  /**
   * 进场特效 true:显示
   */
  public Boolean getShowSpecialEffect() {
    if(showSpecialEffect==null){
        return DEFAULT_SHOWSPECIALEFFECT;
    }
    return showSpecialEffect;
  }

  public boolean hasViewerCount() {
    return viewer_count!=null;
  }

  /**
   * 观众
   */
  public boolean hasViewersList() {
    return viewers!=null;
  }

  /**
   * 0：默认 1：通过附近频道进入
   */
  public boolean hasType() {
    return type!=null;
  }

  /**
   * 进场特效 true:显示
   */
  public boolean hasShowSpecialEffect() {
    return showSpecialEffect!=null;
  }

  public static final class Builder extends Message.Builder<JoinRoomMessage, Builder> {
    public Integer viewer_count;

    public List<Viewer> viewers;

    public Integer type;

    public Boolean showSpecialEffect;

    public Builder() {
      viewers = Internal.newMutableList();
    }

    public Builder setViewerCount(Integer viewer_count) {
      this.viewer_count = viewer_count;
      return this;
    }

    /**
     * 观众
     */
    public Builder addAllViewers(List<Viewer> viewers) {
      Internal.checkElementsNotNull(viewers);
      this.viewers = viewers;
      return this;
    }

    /**
     * 0：默认 1：通过附近频道进入
     */
    public Builder setType(Integer type) {
      this.type = type;
      return this;
    }

    /**
     * 进场特效 true:显示
     */
    public Builder setShowSpecialEffect(Boolean showSpecialEffect) {
      this.showSpecialEffect = showSpecialEffect;
      return this;
    }

    @Override
    public JoinRoomMessage build() {
      return new JoinRoomMessage(viewer_count, viewers, type, showSpecialEffect, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_JoinRoomMessage extends ProtoAdapter<JoinRoomMessage> {
    public ProtoAdapter_JoinRoomMessage() {
      super(FieldEncoding.LENGTH_DELIMITED, JoinRoomMessage.class);
    }

    @Override
    public int encodedSize(JoinRoomMessage value) {
      return ProtoAdapter.UINT32.encodedSizeWithTag(1, value.viewer_count)
          + Viewer.ADAPTER.asRepeated().encodedSizeWithTag(2, value.viewers)
          + ProtoAdapter.UINT32.encodedSizeWithTag(3, value.type)
          + ProtoAdapter.BOOL.encodedSizeWithTag(4, value.showSpecialEffect)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, JoinRoomMessage value) throws IOException {
      ProtoAdapter.UINT32.encodeWithTag(writer, 1, value.viewer_count);
      Viewer.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.viewers);
      ProtoAdapter.UINT32.encodeWithTag(writer, 3, value.type);
      ProtoAdapter.BOOL.encodeWithTag(writer, 4, value.showSpecialEffect);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public JoinRoomMessage decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setViewerCount(ProtoAdapter.UINT32.decode(reader)); break;
          case 2: builder.viewers.add(Viewer.ADAPTER.decode(reader)); break;
          case 3: builder.setType(ProtoAdapter.UINT32.decode(reader)); break;
          case 4: builder.setShowSpecialEffect(ProtoAdapter.BOOL.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public JoinRoomMessage redact(JoinRoomMessage value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.viewers, Viewer.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
