// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: RadioSignal.proto
package com.wali.live.proto.RadioSignal;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Integer;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

/**
 * 12: 房间内push msg_type=801【原房间内push】
 */
public final class RoomMsg extends Message<RoomMsg, RoomMsg.Builder> {
  public static final ProtoAdapter<RoomMsg> ADAPTER = new ProtoAdapter_RoomMsg();

  private static final long serialVersionUID = 0L;

  public static final Integer DEFAULT_INNER_TYPE = 0;

  public static final ByteString DEFAULT_EXT_MSG = ByteString.EMPTY;

  /**
   * 房间消息子类型 1.电台控制消息（DiantaiRoomMsg）
   */
  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#UINT32"
  )
  public final Integer inner_type;

  /**
   * 消息具体内容
   */
  @WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#BYTES"
  )
  public final ByteString ext_msg;

  public RoomMsg(Integer inner_type, ByteString ext_msg) {
    this(inner_type, ext_msg, ByteString.EMPTY);
  }

  public RoomMsg(Integer inner_type, ByteString ext_msg, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.inner_type = inner_type;
    this.ext_msg = ext_msg;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.inner_type = inner_type;
    builder.ext_msg = ext_msg;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof RoomMsg)) return false;
    RoomMsg o = (RoomMsg) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(inner_type, o.inner_type)
        && Internal.equals(ext_msg, o.ext_msg);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (inner_type != null ? inner_type.hashCode() : 0);
      result = result * 37 + (ext_msg != null ? ext_msg.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (inner_type != null) builder.append(", inner_type=").append(inner_type);
    if (ext_msg != null) builder.append(", ext_msg=").append(ext_msg);
    return builder.replace(0, 2, "RoomMsg{").append('}').toString();
  }

  public byte[] toByteArray() {
    return RoomMsg.ADAPTER.encode(this);
  }

  public static final RoomMsg parseFrom(byte[] data) throws IOException {
    RoomMsg c = null;
       c = RoomMsg.ADAPTER.decode(data);
    return c;
  }

  /**
   * 房间消息子类型 1.电台控制消息（DiantaiRoomMsg）
   */
  public Integer getInnerType() {
    if(inner_type==null){
        return DEFAULT_INNER_TYPE;
    }
    return inner_type;
  }

  /**
   * 消息具体内容
   */
  public ByteString getExtMsg() {
    if(ext_msg==null){
        return ByteString.of(new byte[0]);
    }
    return ext_msg;
  }

  /**
   * 房间消息子类型 1.电台控制消息（DiantaiRoomMsg）
   */
  public boolean hasInnerType() {
    return inner_type!=null;
  }

  /**
   * 消息具体内容
   */
  public boolean hasExtMsg() {
    return ext_msg!=null;
  }

  public static final class Builder extends Message.Builder<RoomMsg, Builder> {
    public Integer inner_type;

    public ByteString ext_msg;

    public Builder() {
    }

    /**
     * 房间消息子类型 1.电台控制消息（DiantaiRoomMsg）
     */
    public Builder setInnerType(Integer inner_type) {
      this.inner_type = inner_type;
      return this;
    }

    /**
     * 消息具体内容
     */
    public Builder setExtMsg(ByteString ext_msg) {
      this.ext_msg = ext_msg;
      return this;
    }

    @Override
    public RoomMsg build() {
      return new RoomMsg(inner_type, ext_msg, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RoomMsg extends ProtoAdapter<RoomMsg> {
    public ProtoAdapter_RoomMsg() {
      super(FieldEncoding.LENGTH_DELIMITED, RoomMsg.class);
    }

    @Override
    public int encodedSize(RoomMsg value) {
      return ProtoAdapter.UINT32.encodedSizeWithTag(1, value.inner_type)
          + ProtoAdapter.BYTES.encodedSizeWithTag(2, value.ext_msg)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, RoomMsg value) throws IOException {
      ProtoAdapter.UINT32.encodeWithTag(writer, 1, value.inner_type);
      ProtoAdapter.BYTES.encodeWithTag(writer, 2, value.ext_msg);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RoomMsg decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setInnerType(ProtoAdapter.UINT32.decode(reader)); break;
          case 2: builder.setExtMsg(ProtoAdapter.BYTES.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public RoomMsg redact(RoomMsg value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
