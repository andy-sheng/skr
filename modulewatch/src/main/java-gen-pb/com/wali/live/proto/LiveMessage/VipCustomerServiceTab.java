// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: LiveMessage.proto
package com.wali.live.proto.LiveMessage;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class VipCustomerServiceTab extends Message<VipCustomerServiceTab, VipCustomerServiceTab.Builder> {
  public static final ProtoAdapter<VipCustomerServiceTab> ADAPTER = new ProtoAdapter_VipCustomerServiceTab();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_TITLE = "";

  public static final VipCustomerServiceTabType DEFAULT_TYPE = VipCustomerServiceTabType.VCSTT_NORMAL;

  public static final ByteString DEFAULT_TAB = ByteString.EMPTY;

  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String title;

  @WireField(
      tag = 2,
      adapter = "com.wali.live.proto.LiveMessage.VipCustomerServiceTabType#ADAPTER"
  )
  public final VipCustomerServiceTabType type;

  @WireField(
      tag = 3,
      adapter = "com.squareup.wire.ProtoAdapter#BYTES"
  )
  public final ByteString tab;

  public VipCustomerServiceTab(String title, VipCustomerServiceTabType type, ByteString tab) {
    this(title, type, tab, ByteString.EMPTY);
  }

  public VipCustomerServiceTab(String title, VipCustomerServiceTabType type, ByteString tab,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.title = title;
    this.type = type;
    this.tab = tab;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.title = title;
    builder.type = type;
    builder.tab = tab;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof VipCustomerServiceTab)) return false;
    VipCustomerServiceTab o = (VipCustomerServiceTab) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(title, o.title)
        && Internal.equals(type, o.type)
        && Internal.equals(tab, o.tab);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (title != null ? title.hashCode() : 0);
      result = result * 37 + (type != null ? type.hashCode() : 0);
      result = result * 37 + (tab != null ? tab.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (title != null) builder.append(", title=").append(title);
    if (type != null) builder.append(", type=").append(type);
    if (tab != null) builder.append(", tab=").append(tab);
    return builder.replace(0, 2, "VipCustomerServiceTab{").append('}').toString();
  }

  public byte[] toByteArray() {
    return VipCustomerServiceTab.ADAPTER.encode(this);
  }

  public static final VipCustomerServiceTab parseFrom(byte[] data) throws IOException {
    VipCustomerServiceTab c = null;
       c = VipCustomerServiceTab.ADAPTER.decode(data);
    return c;
  }

  public String getTitle() {
    if(title==null){
        return DEFAULT_TITLE;
    }
    return title;
  }

  public VipCustomerServiceTabType getType() {
    if(type==null){
        return new VipCustomerServiceTabType.Builder().build();
    }
    return type;
  }

  public ByteString getTab() {
    if(tab==null){
        return ByteString.of(new byte[0]);
    }
    return tab;
  }

  public boolean hasTitle() {
    return title!=null;
  }

  public boolean hasType() {
    return type!=null;
  }

  public boolean hasTab() {
    return tab!=null;
  }

  public static final class Builder extends Message.Builder<VipCustomerServiceTab, Builder> {
    public String title;

    public VipCustomerServiceTabType type;

    public ByteString tab;

    public Builder() {
    }

    public Builder setTitle(String title) {
      this.title = title;
      return this;
    }

    public Builder setType(VipCustomerServiceTabType type) {
      this.type = type;
      return this;
    }

    public Builder setTab(ByteString tab) {
      this.tab = tab;
      return this;
    }

    @Override
    public VipCustomerServiceTab build() {
      return new VipCustomerServiceTab(title, type, tab, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_VipCustomerServiceTab extends ProtoAdapter<VipCustomerServiceTab> {
    public ProtoAdapter_VipCustomerServiceTab() {
      super(FieldEncoding.LENGTH_DELIMITED, VipCustomerServiceTab.class);
    }

    @Override
    public int encodedSize(VipCustomerServiceTab value) {
      return ProtoAdapter.STRING.encodedSizeWithTag(1, value.title)
          + VipCustomerServiceTabType.ADAPTER.encodedSizeWithTag(2, value.type)
          + ProtoAdapter.BYTES.encodedSizeWithTag(3, value.tab)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, VipCustomerServiceTab value) throws IOException {
      ProtoAdapter.STRING.encodeWithTag(writer, 1, value.title);
      VipCustomerServiceTabType.ADAPTER.encodeWithTag(writer, 2, value.type);
      ProtoAdapter.BYTES.encodeWithTag(writer, 3, value.tab);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public VipCustomerServiceTab decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setTitle(ProtoAdapter.STRING.decode(reader)); break;
          case 2: {
            try {
              builder.setType(VipCustomerServiceTabType.ADAPTER.decode(reader));
            } catch (ProtoAdapter.EnumConstantNotFoundException e) {
              builder.addUnknownField(tag, FieldEncoding.VARINT, (long) e.value);
            }
            break;
          }
          case 3: builder.setTab(ProtoAdapter.BYTES.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public VipCustomerServiceTab redact(VipCustomerServiceTab value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
