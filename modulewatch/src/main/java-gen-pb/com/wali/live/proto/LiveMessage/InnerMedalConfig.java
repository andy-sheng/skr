// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: LiveMessage.proto
package com.wali.live.proto.LiveMessage;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.WireField;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import okio.ByteString;

public final class InnerMedalConfig extends Message<InnerMedalConfig, InnerMedalConfig.Builder> {
  public static final ProtoAdapter<InnerMedalConfig> ADAPTER = new ProtoAdapter_InnerMedalConfig();

  private static final long serialVersionUID = 0L;

  public static final String DEFAULT_PIC_ID = "";

  @WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING"
  )
  public final String pic_id;

  public InnerMedalConfig(String pic_id) {
    this(pic_id, ByteString.EMPTY);
  }

  public InnerMedalConfig(String pic_id, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.pic_id = pic_id;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.pic_id = pic_id;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof InnerMedalConfig)) return false;
    InnerMedalConfig o = (InnerMedalConfig) other;
    return unknownFields().equals(o.unknownFields())
        && Internal.equals(pic_id, o.pic_id);
  }

  @Override
  public int hashCode() {
    int result = super.hashCode;
    if (result == 0) {
      result = unknownFields().hashCode();
      result = result * 37 + (pic_id != null ? pic_id.hashCode() : 0);
      super.hashCode = result;
    }
    return result;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (pic_id != null) builder.append(", pic_id=").append(pic_id);
    return builder.replace(0, 2, "InnerMedalConfig{").append('}').toString();
  }

  public byte[] toByteArray() {
    return InnerMedalConfig.ADAPTER.encode(this);
  }

  public static final InnerMedalConfig parseFrom(byte[] data) throws IOException {
    InnerMedalConfig c = null;
       c = InnerMedalConfig.ADAPTER.decode(data);
    return c;
  }

  public String getPicId() {
    if(pic_id==null){
        return DEFAULT_PIC_ID;
    }
    return pic_id;
  }

  public boolean hasPicId() {
    return pic_id!=null;
  }

  public static final class Builder extends Message.Builder<InnerMedalConfig, Builder> {
    public String pic_id;

    public Builder() {
    }

    public Builder setPicId(String pic_id) {
      this.pic_id = pic_id;
      return this;
    }

    @Override
    public InnerMedalConfig build() {
      return new InnerMedalConfig(pic_id, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_InnerMedalConfig extends ProtoAdapter<InnerMedalConfig> {
    public ProtoAdapter_InnerMedalConfig() {
      super(FieldEncoding.LENGTH_DELIMITED, InnerMedalConfig.class);
    }

    @Override
    public int encodedSize(InnerMedalConfig value) {
      return ProtoAdapter.STRING.encodedSizeWithTag(1, value.pic_id)
          + value.unknownFields().size();
    }

    @Override
    public void encode(ProtoWriter writer, InnerMedalConfig value) throws IOException {
      ProtoAdapter.STRING.encodeWithTag(writer, 1, value.pic_id);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public InnerMedalConfig decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.setPicId(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            FieldEncoding fieldEncoding = reader.peekFieldEncoding();
            Object value = fieldEncoding.rawProtoAdapter().decode(reader);
            builder.addUnknownField(tag, fieldEncoding, value);
          }
        }
      }
      reader.endMessage(token);
      return builder.build();
    }

    @Override
    public InnerMedalConfig redact(InnerMedalConfig value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
