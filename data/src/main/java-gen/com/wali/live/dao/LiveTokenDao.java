package com.wali.live.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.wali.live.dao.LiveToken;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table LIVE_TOKEN.
*/
public class LiveTokenDao extends AbstractDao<LiveToken, Long> {

    public static final String TABLENAME = "LIVE_TOKEN";

    /**
     * Properties of entity LiveToken.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Uuid = new Property(1, Long.class, "uuid", false, "UUID");
        public final static Property RoomId = new Property(2, String.class, "roomId", false, "ROOM_ID");
        public final static Property RetryTimes = new Property(3, Integer.class, "retryTimes", false, "RETRY_TIMES");
        public final static Property Token = new Property(4, String.class, "token", false, "TOKEN");
    };


    public LiveTokenDao(DaoConfig config) {
        super(config);
    }
    
    public LiveTokenDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'LIVE_TOKEN' (" + //
                "'_id' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "'UUID' INTEGER," + // 1: uuid
                "'ROOM_ID' TEXT," + // 2: roomId
                "'RETRY_TIMES' INTEGER," + // 3: retryTimes
                "'TOKEN' TEXT);"); // 4: token
        // Add Indexes
        db.execSQL("CREATE INDEX " + constraint + "IDX_LIVE_TOKEN_UUID ON LIVE_TOKEN" +
                " (UUID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_LIVE_TOKEN_ROOM_ID ON LIVE_TOKEN" +
                " (ROOM_ID);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'LIVE_TOKEN'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, LiveToken entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long uuid = entity.getUuid();
        if (uuid != null) {
            stmt.bindLong(2, uuid);
        }
 
        String roomId = entity.getRoomId();
        if (roomId != null) {
            stmt.bindString(3, roomId);
        }
 
        Integer retryTimes = entity.getRetryTimes();
        if (retryTimes != null) {
            stmt.bindLong(4, retryTimes);
        }
 
        String token = entity.getToken();
        if (token != null) {
            stmt.bindString(5, token);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public LiveToken readEntity(Cursor cursor, int offset) {
        LiveToken entity = new LiveToken( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // uuid
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // roomId
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // retryTimes
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // token
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, LiveToken entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUuid(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setRoomId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setRetryTimes(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setToken(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(LiveToken entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(LiveToken entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
